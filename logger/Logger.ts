//  Winston can be used to log Playwright test executions, capturing details about test runs, errors, and debugging information.
import winston from 'winston';

declare const Logger: winston.Logger;
export default Logger;
